# 함께 만들기

## 내 수업에 맞게 고쳐 쓰기

`skill/assets/build.py` 상단의 상수만 바꾸면 학년 수준이 바뀝니다.
색·여백·모서리는 `skill/assets/template.html` 의 `:root` 토큰에 모여 있습니다.
**새 색을 추가하지 않는 것**이 이 시스템의 유일한 규칙입니다.

## 새 레이아웃을 더할 때

1. `skill/assets/build.py` 의 `render()` 에 `type` 분기를 추가합니다
2. `skill/references/spec.md` 의 type 표에 한 줄 적습니다
3. `examples/` 의 명세에 넣어 빌드하고, `shoot.py` 로 캡처해 **눈으로 확인합니다**

빌드 경고 0건과 캡처 확인, 두 가지를 통과하면 됩니다.

## 이슈로 알려주시면 좋은 것

- 다른 학년군에서 써 보고 조정한 숫자
- 넘치거나 깨진 슬라이드 (캡처와 함께)
- 교실에서 아쉬웠던 조작
